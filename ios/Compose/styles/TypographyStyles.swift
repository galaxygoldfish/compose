import SwiftUI
import Foundation

let typographyH1 = Font.custom(InterBold, size: 40)
let typographyH2 = Font.custom(InterBold, size: 36)
let typographyH3 = Font.custom(InterBold, size: 33)
let typographyH4 = Font.custom(InterBold, size: 26)
let typographyH6 = Font.custom(InterSemiBold, size: 19)

let typographyBody1 = Font.custom(InterRegular, size: 16)
let typographyBody2 = Font.custom(InterRegular, size: 16)

let typographySubtitle1 = Font.custom(InterRegular, size: 14)
let typographySubtitle2 = Font.custom(InterSemiBold, size: 16)

let typographyButton = Font.custom(InterSemiBold, size: 16)
let typographyOverline = Font.custom(InterBold, size: 14)
let typographyCaption = Font.custom(InterRegular, size: 12)
